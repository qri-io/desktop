{
  "compilerOptions": {
    "sourceMap": true,
    "target": "es5",
    "jsx": "react",
    // umd complies the code so that it can be read in the browser
    // coupled with the field "libraryTarget: 'umd'" inside the `webpack.config.webapp.js` file
    // the more common `commonjs` cannot be read in the browser, but instead is made for node
    "module": "umd",
    "moduleResolution": "node",
    "experimentalDecorators": true,
    "declaration": false,
    "removeComments": true,
    "noImplicitReturns": true,
    "noUnusedLocals": true,
    "strict": true,
    "outDir": "build",
    "lib": ["es6", "es7", "dom"],
    "baseUrl": "src",
    "paths": {
      "app/*": ["./app/*"]
    },
    "allowSyntheticDefaultImports": true
  },
  "exclude": ["dist", "node_modules"]
}
